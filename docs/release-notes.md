# Release Notes

## Current GitHub build

- Fixed global installation from the public GitHub archive by shipping the compiled `dist` runtime.
- Removed the Git dependency preparation hook that required `tsc` before dev dependencies were available.
- Added a compact original Garuda terminal mark to the TUI header.
- Added navigable slash-command filtering with Up/Down selection and Tab completion.
- Added first-run provider setup and phased upgrade documentation.

The project contains original Garuda source and UI implementation. Provider names, API endpoint metadata, and compatibility concepts remain subject to each provider's terms. No third-party logos or copied source code are bundled.